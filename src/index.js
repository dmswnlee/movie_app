import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(
<App />, //app 컴포넌트를 그려라 이 app컴포넌트는 app.js 내용!
document.getElementById('root') // index.html에 있는 root에 
);
